尝试了很久，没做到在开发模式获取到md文件文本内容，所以就群不塞到了`defaultPrompts.ts`里面了，其实我并不喜欢这么干，这个文件夹只是为了方便查找提示词的

Tried many times, but couldn't get the md file text content in development mode, so I put it in the `defaultPrompts.ts` folder. I don't like this either, this folder is just for finding prompt words.